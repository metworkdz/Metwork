# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
.env.development
.env.production

# vercel
.vercel

# local db + DB snapshots (never commit data dumps)
.local-db.json
/backups/

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/

# Prisma
/prisma/migrations/dev/

# Docker
docker-compose.override.yml

# Local mock data store (used by the dev auth API)
/.data/

# Admin-uploaded media (mentor avatars, etc.) — replace with S3 in prod
/public/uploads/

# Claude Code — worktrees and session files (never commit these)
.claude/worktrees/
SESSION_*.md

# e2e email observability sink (USE_LOCAL_DB only)
.e2e-emails.jsonl
.env*

# One-off campaign HTML previews (scripts/campaigns --test)
scripts/campaigns/.preview*.html
